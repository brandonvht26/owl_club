// src/pages/NotFoundPage.jsx
import React from 'react';
import NotFound from '../components/NotFound/NotFound'; // <-- Importamos desde la nueva ubicación

const NotFoundPage = () => {
    return (
        <NotFound />
    );
};

export default NotFoundPage;